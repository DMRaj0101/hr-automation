# ==========================
# Core (kept)
# ==========================
fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
pydantic==2.9.2
requests==2.32.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
python-dotenv==1.0.1

# ==========================
# HR Assistant / Knowledge Agent RAG (kept)
# ==========================
sentence-transformers==3.2.1
transformers==4.46.3
torch==2.5.1
faiss-cpu==1.9.0
numpy==2.1.3
markdown==3.7

# ==========================
# TODO -- new integration dependencies, add as each connector is
# implemented (owner picks the right client library, these are
# suggestions based on what each vendor publishes):
# ==========================
# python-keycloak            # Identity Agent -> Keycloak Admin REST API
# requests (already above)   # Kimai + Snipe-IT both have plain REST/token APIs, no special client needed
# TODO: confirm MailU integration approach (admin API vs mail/ CLI vs direct DB) -- see integrations/mailu_connector.py
